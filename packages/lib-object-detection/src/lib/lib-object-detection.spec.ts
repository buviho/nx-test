import { libObjectDetection } from './lib-object-detection';

describe('libObjectDetection', () => {
  it('should work', () => {
    expect(libObjectDetection()).toEqual('lib-object-detection');
  });
});
